# requirements_api.txt
# Dependencias para api_server.py + scraper_pipeline.py
# Instalar con: pip install -r requirements_api.txt

# API (versiones fijas para evitar incompatibilidades)
# Alineadas con requirements.txt — verificadas contra la suite de tests en agosto 2026.
fastapi==0.138.2
uvicorn[standard]==0.49.0

# Archivos estaticos — REQUERIDO por FastAPI StaticFiles
aiofiles>=23.2.1

# Scraping robusto
lxml==6.1.1
html5lib>=1.1

# Manejo de fechas (para pipeline)
python-dateutil>=2.9.0

# Agentic AI (opcional — se degrada si no hay ANTHROPIC_API_KEY)
anthropic>=0.40.0